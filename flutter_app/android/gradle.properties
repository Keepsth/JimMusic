org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This newDsl flag was added by the Flutter template (AGP 9 uses the legacy DSL
# interface that the Flutter Gradle Plugin reads).
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template (use the standalone
# Kotlin Gradle Plugin applied by app/build.gradle.kts).
android.builtInKotlin=false
