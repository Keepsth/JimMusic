[package]
name = "ui-bridge"
version = "2.0.0"
edition = "2021"
description = "JimMusic UI 桥插件（FFI 接口与 Flutter/ArkUI 通信）"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
plugin-abi = { path = "../../plugin-abi" }
