[package]
name = "alsa-output"
version = "2.0.0"
edition = "2021"
description = "JimMusic Linux ALSA/PipeWire 真实音频输出插件（cpal 后端）"

[lib]
name = "alsa_output"
crate-type = ["cdylib", "rlib"]

[dependencies]
plugin-abi = { path = "../../plugin-abi" }
serde_json = "1"
cpal = "0.18"
